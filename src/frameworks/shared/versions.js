// @ts-check
'use strict';

/**
 * Version/port constants used by the official generators. Centralized here
 * so they're defined exactly once instead of being copy-pasted across
 * generator files.
 */
module.exports = {
  ASPNET_DOTNET_FRAMEWORK: 'net8.0',
  ASPNET_DEFAULT_PORT: 5000,
  SPRING_BOOT_JAVA_VERSION: '21',
};
